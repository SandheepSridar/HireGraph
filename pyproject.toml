[project]
name = "hiregraph"
version = "0.1.0"
description = "Multi-agent AI hiring intelligence platform automating resume parsing, JD matching, bias detection, and candidate ranking using LangGraph and structured LLM outputs"
readme = "README.md"
requires-python = ">=3.11"
dependencies = [
    "ipython>=9.15.0",
    "langchain>=1.3.11",
    "langchain-openai",
    "langgraph",
    "langgraph-cli[inmem]",
]
